"""Deployment adapter selection for {{ project_name }}.

Azure AI Agent Service. Requires:

    pip install langgraph-forge[azure]

Per-adapter configuration (endpoint, agent_id, credential) is passed
through :class:`AdapterConfig.extra` at ``prepare`` time.

Note: v0.1 ships the Protocol contract only; concrete Azure integration
lands in v0.2 -- calling ``prepare`` / ``invoke`` raises
``NotImplementedError`` until then.
"""

from __future__ import annotations

from langgraph_forge.deploy.azure import AzureAIAgentAdapter as Adapter

ADAPTER = Adapter()
